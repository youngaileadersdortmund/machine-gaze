import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["since-trans-mines-friday.trycloudflare.com", "unmeretriciously-dreariest-jenelle.ngrok-free.dev", "http://127.0.0.1:4040"],
};

export default nextConfig;